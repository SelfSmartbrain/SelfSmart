import { Select } from "@base-ui/react/select";
import { cn } from "@/lib/utils";

const { Root, Trigger, Value, Popup, Item } = Select;

export { 
  Root as Select, 
  Trigger as SelectTrigger, 
  Value as SelectValue, 
  Popup as SelectContent, 
  Item as SelectItem 
};